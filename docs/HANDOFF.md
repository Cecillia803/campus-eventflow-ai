# 下一台电脑交接

## 当前阶段
本目录只有源代码和Markdown。没有构建配置、依赖、业务数据、数据库或运行服务。
桌面另一目录/旧Documents骨架不属于本次交付，请不要合并其中的.env或启动脚本。
本次上传已获用户授权，Git远程仓库为Cecillia803/campus-eventflow-ai（私有）；以下环境与运行接入仍未实施。

## 需要接入的Adapter（都尚未实现）
1. Java/前端构建与启动入口：先选定版本，配置模块依赖，再编译。
2. 身份：服务端认证、角色、会话归属、CSRF/限流与日志脱敏；App接入身份切换时必须重建client/清空页面状态。
3. 活动查询与详情：真实持久模型、分页排序、日期与时区、来源覆盖范围。
4. 来源读取：逐渠道权限验证、增量同步、采集失败管理、SSRF与正文净化。
5. 提取/去重：OCR/模型、schema校验、字段证据定位、真实语料评测。
6. 审核存储：乐观版本、增量修订、审核审计和发布事件。
7. 报名存储：事务、唯一约束、幂等结果、队列序号、状态与事件原子持久化。
8. 候补触发：调用reconcile并持久化其结果，处理截止、恢复与事件重复。
9. 通知：订阅存储、调度、站内收件箱、幂等投递和退订检查。
10. Agent：Planner、ActivityTools、Retriever、Model、Conversations及模型预算。
11. 确认草稿：DraftService.Store、ID分配、有效时长、ConfirmationPolicy.Executor与报名原子/幂等协调。
12. 管理查询与任务处理：来源、审核、审计、场次创建、重试权限/审计。
13. EventFlowClient：身份化传输、响应校验、错误映射、取消信号、时区。
14. 前端挂载和导航：现有App使用组件内导航；如增加URL路由需另做权限与直达页面测试。

## 顺序（不是当前执行命令）
A. 只读源码与docs，确认接口与业务决定；保留未验证标记。
B. 在主要开发电脑上经授权创建构建清单、安装依赖，编译并运行纯规则测试。
C. 实现MySQL正确性基线和身份，跑通活动列表/详情与模拟报名。
D. 补订阅、审核与站内提醒，再接来源渠道与资料提取。
E. 接只读Agent与会话；最后接确认草稿执行，不让模型直接写报名数据。
F. 根据压测瓶颈决定Redis/Kafka优化，不直接宣称整场次快照算法高性能。
G. 加跨系统故障验证、提示注入/越权测试、真实样本AI评测、浏览器视觉与交互验收。
H. 用户明确授权后再初始化/推送GitHub；不能自动把本机配置与业务资料放进仓库。

## 不确定/待产品确认
- 校园真实渠道的授权与内容使用范围。
- 日期精度、来源冲突权威规则和更换证据的审核界面。
- 字段删除语义、批量通知摘要策略与后台分页。
- 候补资格改变时如何跳过；当前按入队时判定，不能默认为动态资格。
- 允许取消后重报、自动递补等已按本次技术方案采用，若变更需更新规则与验收。
- 正式样本、模型及性能目标，不在本机生成或承诺。

## 前端限制
无业务数据时只显示未接入，不生成卡片或账号。
客户端对象身份变更必须销毁旧页面请求与会话；不要只修改同一对象内部token。
命令ID在页面内存中维持一次操作的重试身份，不是持久化业务数据存储；
刷新/离开页面后应查询服务端当前状态再发起新操作。
会话目前为页面内选择，无会话列表/持久导航；可通过client的已有会话接口扩展。
没有图像资产、CDN字体、构建入口、打包或浏览器截图。

## 事实与承诺
已写规则 ≠ 已编译。
已写状态转换 ≠ 数据库并发正确。
已写模型端口 ≠ AI已接入。
已有引用ID校验 ≠ 回答已被事实验证。
现有UI源文件 ≠ 页面已通过视觉/移动端验收。
