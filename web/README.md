# 前端源码交接

Vue 3＋TypeScript源文件，框架依赖尚未安装，未构建或渲染。
没有package.json、Vite配置、HTML入口、main.ts、URL路由、API地址或Mock数据。

App接收可选EventFlowClient；没有它会显示“未接入”。
下一台电脑创建可信传输Adapter并注入；不要在页面里直接fetch固定地址。
来源和消息按普通文本展示，外链只接受https显示。后端仍必须独立做输入与访问安全校验。

页面内的导航/表单状态不是业务存储，不使用localStorage、假账号或演示记录。
运行时用户点击产生的命令key只用于同一操作重试；本次没有执行页面或生成业务记录。
前端禁用按钮不代替服务端鉴权/幂等。

审核字段修改和模拟场次创建表单已写，但响应/传输Adapter、后台权限和持久处理后置。
活动助手页面已写会话创建与发送编排；模型/历史持久化由服务端实现，不提供假回答。

参考docs/HANDOFF和contracts/README完成接入、契约映射、分页与真实浏览器验收。

